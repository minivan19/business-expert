---
name: business-expert
description: 商务专家 - 360°客户档案与经营分析。基于客户档案数据生成经营分析报告，采用混合模式（脚本处理结构化数据+LLM智能分析）。
dependency:
  python:
    - pandas>=2.0.0
    - openpyxl>=3.0.0
    - requests>=2.28.0
---

# 商务专家 Skill 使用指南

## 任务目标
- 本 Skill 用于：基于客户档案数据生成360°客户档案与经营分析报告
- 能力包含：
  - 脚本处理：读取源数据，直接生成结构化表格
  - 智能体：仅对需要分析的部分调用LLM
- 触发条件：用户需要生成客户经营分析报告时

## 前置准备
- 依赖说明：
  ```
  pandas>=2.0.0
  openpyxl>=3.0.0
  requests>=2.28.0
  ```

## 数据来源

### 输入路径
```
C:\Users\mingh\client-data\raw\客户档案\{客户}\
├── 基础数据/              ← 客户主数据.xlsx
├── 订阅合同行/            ← 订阅明细.xlsx
├── 实施合同行/            ← 固定金额台账.xlsx + 人天框架台账.xlsx
├── 订阅合同收款情况/      ← 订阅合同收款情况.xlsx
└── 运维工单/             ← 运维工单.xlsx（多个文件）
```

### 6层数据框架

| Part | 名称 | 数据来源 | 处理方式 |
|------|------|---------|---------|
| 1 | 客户基础档案 | 客户主数据（139字段） | 脚本直接处理 |
| 2 | 订阅续约与续费 | 订阅明细+收款数据 | 2.1-2.3脚本，2.4 LLM |
| 3 | 实施优化情况 | 固定金额+人天框架 | 3.1/3.3/3.4脚本，3.2 LLM |
| 4 | 运维情况 | 运维工单 | 脚本直接处理 |
| 5 | 客户经营信息 | 网络搜索 | 待开发 |
| 6 | 综合经营分析 | LLM综合分析 | LLM |

## 核心架构：混合模式

### 设计理念
- **结构化数据**：脚本直接读取Excel，生成表格（准确、高效）
- **智能分析**：仅对需要推理的部分调用LLM（降低成本、减少幻觉）

### 处理流程

```
┌─────────────────────────────────────────────────────┐
│                   数据加载                           │
│  客户主数据 | 订阅明细 | 收款数据 | 实施数据 | 运维工单 │
└─────────────────────────────────────────────────────┘
                          │
         ┌────────────────┼────────────────┐
         ▼                ▼                ▼
    ┌─────────┐    ┌─────────┐    ┌─────────┐
    │ Part 1  │    │ Part 2  │    │ Part 3  │
    │(脚本)   │    │ 2.1-2.3 │    │3.1/3.3/│
    │         │    │(脚本)   │    │ 3.4(脚本)│
    └─────────┘    └─────────┘    └─────────┘
                           │                │
                           ▼                ▼
                    ┌──────────┐      ┌──────────┐
                    │  Part 2  │      │  Part 3  │
                    │  2.4智能│      │  3.2智能 │
                    │  (LLM)  │      │  (LLM)   │
                    └──────────┘      └──────────┘
                           │                │
                           ▼                ▼
                    ┌──────────────────────┐
                    │      Part 4         │
                    │    运维情况(脚本)    │
                    └──────────────────────┘
                           │
                           ▼
                    ┌──────────────────────┐
                    │      Part 6         │
                    │    综合分析(LLM)     │
                    └──────────────────────┘
```

## LLM Prompt 约束规范

### 关键约束（必须遵守）

1. **禁止Markdown格式标记**
   - 禁止使用 `**` 加粗
   - 禁止使用 `##` 标题标记
   - 禁止使用 `-` 列表（用纯文本描述）

2. **禁止重复标题**
   - Prompt中不要包含 `## xxx` 标题
   - 只输出内容本身，标题由代码框架控制

3. **禁止客套话**
   - 禁止使用 "好的"、"作为您的"、"我已"、"这是" 等

4. **输出格式**
   - 使用纯文本或简单列表
   - 如需列表，用 "1. " 编号格式，不要用 "-"

### Prompt模板示例

```
你是一个客户成功经理助手。根据以下数据，进行智能分析。

## 重要约束
- 禁止使用**加粗**等Markdown格式
- 禁止使用##标题标记
- 禁止使用"好的"、"作为您的"等客套话
- 直接输出分析内容，不要任何开场白

## 数据摘要
{数据内容}

请分析以下内容（直接输出，不要带标题）：
1. 分析要点1
2. 分析要点2
```

## 使用方式

### 生成完整报告
```bash
cd skills/business-expert
python scripts/generate_full_report_v2.py
```
- 输入客户编号或 'all'
- 输出：{客户}_经营分析报告.md

## 文件结构

```
business-expert/
├── SKILL.md                          ← 本文件
├── scripts/
│   ├── generate_data_framework.py    ← 数据框架生成（旧版）
│   ├── generate_full_report.py       ← 完整报告生成（v1）
│   └── generate_full_report_v2.py    ← 完整报告生成（v2混合模式）
└── references/
    └── deterministic_analysis_manual.md  ← 确定性手册
```

## 输出示例

### Part  2.4 智能分析（正确格式）
```
续约分析：
1. 订阅费用时间阶段变化...
2. 续约/降价原因分析...

续费分析：
1. 收款进度评估...
2. 逾期风险分析...
```

### Part 3.2 智能分析（正确格式）
```
1. 实施优化费用年度变化趋势...
2. 实施模式分析...
3. 预测和建议...
```

### Part 6 综合经营分析（正确格式）
```
6.1 客户价值分级
价值等级：A级
评估依据：...

6.2 经营健康度评估
订阅健康度：良好
收款健康度：一般
运维健康度：良好

6.3 机会分析
增购机会：...

6.4 风险预警
流失风险：低
收款风险：中

6.5 下一步行动建议
短期：1. xxx 2. xxx
中期：1. xxx 2. xxx
```
